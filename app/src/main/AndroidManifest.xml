<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_DOCUMENTS" />

    <!-- ✅ נראות לאפליקציות מפות / סכימות ניווט (Android 11+) -->
    <queries>
        <package android:name="com.google.android.apps.maps" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProjectAppraisal"
        tools:targetApi="tiramisu">

        <!-- ⛳️ זה ה-Launcher החדש -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.FinalProjectAppraisal.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- מסך הבית (היעד אחרי הספלש) -->
        <activity
            android:name=".activity.HomePageActivity"
            android:exported="true" />

        <!-- ❌ להסיר Intent-Filter מ-MainActivity כדי שלא יעלה ראשון -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="false" />

        <!-- שאר האקטיביטיז (ללא MAIN/LAUNCHER) -->
        <activity
            android:name=".activity.newProject.presenter.PresenterDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activity.AllProjects.AllProjectsViewActivity"
            android:exported="false" />
        <activity
            android:name=".activity.AllProjects.ProjectPreviewActivity"
            android:exported="false" />
        <activity
            android:name=".activity.newProject.property.activity.AdditionalFacilitiesActivity"
            android:exported="false" />
        <activity
            android:name=".activity.newProject.property.activity.PropertyDescriptionActivity"
            android:exported="false" />
        <activity
            android:name=".activity.newProject.bank.BankDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".activity.newProject.images.UploadImagesActivity"
            android:exported="false" />
        <activity
            android:name=".activity.newProject.client.ClientDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activity.newProject.property.activity.ApartmentDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activity.newProject.property.activity.PropertyDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activity.myProjects.MyProjectsActivity"
            android:exported="false" />
        <activity
            android:name=".activity.SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".activity.newProject.bank.EditBankDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activity.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".activity.AppraiserSetupActivity"
            android:exported="true" />
        <activity android:name=".activity.SettingsActivity" />
        <activity android:name=".activity.activitiesAdmin.AdminListActivity" />
        <activity android:name=".activity.EditProfileActivity" />

        <!-- 🔑 Google Maps/Places API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- 📸 FILE PROVIDER לצילום תמונה מהמצלמה -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
