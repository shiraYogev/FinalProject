<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rowRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layoutDirection="rtl"
    android:orientation="vertical"
    android:gravity="end"
    android:paddingStart="32dp"
    android:paddingEnd="32dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- כותרת סעיף (מופיע רק כש-isHeader=true) -->
    <TextView
        android:id="@+id/txtHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:textAlignment="viewEnd"
        android:textStyle="bold"
        android:textSize="18sp"
        android:textColor="#111827"
        android:visibility="gone"
        android:paddingTop="16dp"
        android:paddingBottom="8dp"
        tools:visibility="visible"
        tools:text="פרטי בנק" />

    <!-- שורה ישנה (לגב-קומפטביליות) -->
    <TextView
        android:id="@+id/txtLine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:textAlignment="viewEnd"
        android:textSize="15sp"
        android:textColor="#374151"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:visibility="gone"
        tools:text="ריצוף : פורצלן גרניט (60×60)" />

    <!-- שורת Key/Value – שניהם מיושרים לימין -->
    <LinearLayout
        android:id="@+id/kvRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layoutDirection="rtl"
        android:gravity="end|center_vertical"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <!-- המפתח (label) – בצד ימין -->
        <TextView
            android:id="@+id/txtKey"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:textAlignment="viewEnd"
            android:textSize="16sp"
            android:textColor="#6B7280"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="ריצוף" />

        <!-- רווח קטן בין מפתח לערך -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=" : "
            android:textSize="16sp"
            android:textColor="#6B7280" />

        <!-- ערך – גם הוא מיושר לימין -->
        <TextView
            android:id="@+id/txtValue"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="end"
            android:textAlignment="viewEnd"
            android:textSize="16sp"
            android:textColor="#374151"
            android:maxLines="3"
            android:ellipsize="end"
            android:layout_marginStart="4dp"
            tools:text="פורצלן גרניט (60×60)" />
    </LinearLayout>

    <!-- קו מפריד -->
    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#EEF0F3"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"/>
</LinearLayout>
